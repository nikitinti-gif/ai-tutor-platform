# Рабочий журнал AI Tutor Platform

Этот файл фиксирует только фактически выполненную работу. Идеи и будущие функции хранятся в `BACKLOG.md`, отложенные решения — в `Release/NOT_NOW.md`.

## Правила ведения

- Одна запись — одна завершённая рабочая сессия или проверяемое изменение.
- Не считать функцию готовой без указанной проверки.
- Если end-to-end проверка не выполнена, ставить статус «требует проверки».
- Записывать принятые решения, обнаруженные ошибки и следующий конкретный шаг.
- Не переносить сюда длинные планы и нереализованную архитектуру.

## Формат записи

### ГГГГ-ММ-ДД — краткое название работы

- Цель:
- Что изменено:
- Затронутые файлы:
- Проверка:
- Результат:
- Статус:
- Следующий шаг:

## Записи

### 2026-07-18 — фоновая обработка синтетической очереди Gemini

- Цель: автоматически обработать принятую синтетическую работу и показать
  результат только преподавателю.
- Что изменено: добавлен startup-worker, атомарный захват PostgreSQL-записи,
  загрузка фото из Telegram, Gemini Vision + текстовая проверка, сохранение
  JSONB-анализа и отдельная доставка результата преподавателю.
- Политика: выбираются исключительно `accepted` и `is_synthetic=TRUE`;
  реальные работы не отправляются во внешний AI.
- Надёжность: `FOR UPDATE SKIP LOCKED`, максимум две попытки, возврат в
  `accepted` после временной ошибки и `failed` после исчерпания попыток.
- Конфигурация Render: worker включён, интервал 15 секунд, две попытки.
- Проверка: 17 целевых тестов OK; изменённые Python-файлы компилируются.
- Статус: код готов, требуется end-to-end проверка на Render и бесплатном
  лимите Gemini.
- Ограничение: отдельное условие задачи пока не хранится; модель использует
  только явно видимое на фото, иначе должна вернуть `unclear`.
- Следующий шаг: отправить одну новую синтетическую работу и проверить
  переход `accepted → processing → teacher_review` и сообщение учителю.

### 2026-07-18 — однопользовательский режим тестовой семьи

- Цель: проверить роли преподавателя, родителя и ученика с одного
  администраторского Telegram-аккаунта.
- Что изменено: добавлены две кнопки администратора — создание тестовой семьи
  и отправка тестовой работы; постоянная роль преподавателя не меняется.
- Изоляция: синтетический ученик имеет отрицательный ID, а работа сохраняется
  с `is_synthetic=TRUE` и не считается реальной.
- Миграция: поле добавляется существующей таблице через безопасный
  `ADD COLUMN IF NOT EXISTS`.
- Проверка: целевые тесты сервиса, репозиториев и качества фото проходят;
  требуется end-to-end прогон на Render.
- Следующий шаг: создать тестовую семью, употребить код один раз и отправить
  фото через кнопку тестовой работы.

### 2026-07-18 — одноразовая привязка родителя к ученику

- Цель: исключить запись анализа в профиль чужого ученика.
- Что изменено: преподаватель создаёт код для зарегистрированного ученика;
  родитель вводит его и получает связь в PostgreSQL.
- Безопасность: код действует 30 минут, используется один раз и хранится
  только как SHA-256-хеш; неоднозначные символы исключены.
- Затронутые файлы: слой `family_link`, обработчики родителя и преподавателя,
  состояния, клавиатуры, тесты и документация.
- Проверка: 14 целевых тестов OK, изменённые Python-файлы компилируются.
- Результат: новая работа содержит `student_telegram_id`; загрузка без
  привязки блокируется.
- Статус: код готов, требуется ручной end-to-end прогон на Render.
- Ограничение: пилот поддерживает одного активного ребёнка на родителя;
  прежние записи с `student_telegram_id=NULL` не назначаются автоматически.
- Следующий шаг: проверить создание, однократное использование и истечение
  кода, затем подключить worker для статуса `accepted`.

### 2026-07-18 — PostgreSQL-очередь родительских работ

- Цель: не терять принятую фотографию при перезапуске Render.
- Что изменено: добавлена таблица `homework_submissions`; работа записывается
  до подтверждения родителю, а успешное уведомление преподавателя получает
  отметку `teacher_notified_at`.
- Затронутые файлы: `src/database/submission_storage.py`,
  `src/repositories/submission_repository.py`, родительский обработчик и тесты.
- Проверка: новые тесты репозитория и тесты качества фото — 7/7 OK.
- Результат: ошибка БД не даёт ложного подтверждения; ошибка Telegram не
  удаляет уже сохранённую работу.
- Статус: код готов, требуется end-to-end проверка на Render/PostgreSQL.
- Ограничение: связь родителя с ребёнком ещё отсутствует, поэтому
  `student_telegram_id` хранится как `NULL`; AI-обработчик не запускается.
- Следующий шаг: проверить запись на Render, затем добавить восстановление и
  фоновую обработку статуса `accepted`.

### 2026-07-13 — введено системное ведение проектной истории

- Цель: отделить фактическую историю разработки от backlog и планов релизов.
- Что изменено: создан рабочий журнал; определено правило обновления журнала после содержательных сессий.
- Затронутые файлы: `docs/WORK_LOG.md`, `docs/BUSINESS_AUTOMATION_LEARNING_PROGRAM.md`, `docs/CLAUDE.md`.
- Проверка: файлы созданы в папке `docs` и доступны через Google Drive.
- Результат: у проекта появился единый хронологический журнал выполненной работы.
- Статус: выполнено.
- Следующий шаг: при исправлении `check_homework_text` записать изменение и результат ручного end-to-end прогона.

### 2026-07-13 — зафиксированы три антипаттерна ведения проекта

- Цель: не повторять расширение до завершения ядра, потерю контекста и преждевременное объявление кода финальным.
- Что изменено: в источник правды добавлены три антипаттерна и обязательные правила; в учебную программу добавлен практический урок со шкалой готовности.
- Затронутые файлы: `docs/CLAUDE.md`, `docs/BUSINESS_AUTOMATION_LEARNING_PROGRAM.md`, `docs/WORK_LOG.md`.
- Проверка: обновлённые файлы повторно прочитаны из Google Drive.
- Результат: прошлые слабые места преобразованы в проверяемые правила работы.
- Статус: выполнено.
- Следующий шаг: применять правила при исправлении `check_homework_text`, не расширяя текущую фазу.

### 2026-07-13 — контрольный просмотр после полной синхронизации

- Цель: сверить источник правды с актуальными файлами Google Drive.
- Что проверено: структура проекта, `docs/CLAUDE.md`, `src/ai_engine/homework_checker.py`, `llm_client.py`, `parser.py`, `prompts.py`, `schemas.py`, путь вызова из `telegram_bot/handlers/student.py`.
- Обнаружено: `LLMClient.check_homework_text()` уже существует; прежний `AttributeError` больше не подтверждается статическим просмотром. Реальный LLM API не подключён — используется `if/else`-заглушка. Папка `tests` пуста.
- Проверка: выполнено чтение актуальных файлов из Google Drive; запуск бота и ручной end-to-end сценарий не выполнялись.
- Результат: источник правды приведён в соответствие с кодом.
- Статус: требует проверки.
- Следующий шаг: выполнить текстовый пользовательский сценарий и зафиксировать фактический результат.

### 2026-07-13 — ручной текстовый сценарий ученика выполнен

- Цель: проверить основной путь отправки текстового решения после синхронизации.
- Сценарий: ученик открыл проверку решения, отправил текст «не знаю, как решить задачу» и получил ответ со статусом, обратной связью, рекомендацией, причиной и планом.
- Проверка: бот вернул результат без `AttributeError`; цепочка проверки и педагогического решения выполнилась. После ручной остановки `Ctrl+C` Python показал `CancelledError` и `KeyboardInterrupt`, что относится к завершению polling, а не к обработке решения.
- Обнаружено: внутреннее решение «не отправлять ученику автоматический вывод» фактически показывается ученику вместе с причиной и планом; формат ответа нужно разделить на ученический и преподавательский.
- Результат: Фаза 0 завершена на текущей `if/else`-заглушке.
- Статус: выполнено.
- Следующий шаг: Фаза 1 — подключить реальный LLM и затем проверить 15–20 примеров по информатике.

### 2026-07-13 — Gemini подготовлен для синтетической оценки

- Цель: заменить `if/else`-имитацию реальным LLM-вызовом без передачи реальных ученических данных.
- Что изменено: `llm_client.py` переведён на официальный `google-genai` и модель `gemini-2.5-flash-lite`; `homework_checker.py` получил границу `synthetic_test`; в `schemas.py` добавлена JSON-схема ответа; в `requirements.txt` добавлен `google-genai`; созданы unit-тест и запуск оценки на 15 искусственных примерах по информатике.
- Политика данных: обычный Telegram-сценарий не вызывает Gemini; внешний вызов разрешён только при явном `synthetic_test=True`.
- Затронутые файлы: `src/ai_engine/llm_client.py`, `homework_checker.py`, `schemas.py`, `requirements.txt`, `tests/test_homework_checker.py`, `tests/run_gemini_evaluation.py`.
- Проверка: Python-синтаксис всех подготовленных файлов проверен локально. Установка SDK, unit-тесты и реальный Gemini API-вызов ещё не выполнялись.
- Результат: интеграция сохранена как безопасный тестовый прототип.
- Статус: требует проверки.
- Следующий шаг: установить зависимости, добавить локальный ключ и выполнить 15 синтетических проверок.

### 2026-07-13 — исправлена загрузка Gemini-ключа из `.env`

- Цель: устранить `LLMConfigurationError` при запуске тестового скрипта.
- Причина: `llm_client.py` читал переменную окружения, но тестовый запуск не импортировал `config.py`, где ранее вызывался `load_dotenv()`.
- Что изменено: загрузка `.env` добавлена непосредственно в `src/ai_engine/llm_client.py`.
- Проверка: Python-синтаксис файла проверен; повторный запуск с локальным ключом должен выполнить пользователь.
- Статус: требует проверки.
- Следующий шаг: синхронизировать файл и повторно запустить `python tests/run_gemini_evaluation.py`.

### 2026-07-13 — модель Gemini обновлена после реального API-ответа

- Цель: устранить `404 NotFoundError` при первом синтетическом запросе.
- Фактический ответ API: `gemini-2.5-flash-lite is no longer available to new users`.
- Что изменено: модель по умолчанию заменена на `gemini-3.1-flash-lite`.
- Проверка: ключ и сетевое соединение дошли до Gemini API; повторный модельный запрос ещё не выполнен.
- Статус: требует проверки.
- Следующий шаг: изменить локальную переменную `GEMINI_MODEL` и повторить оценку.

### 2026-07-13 — прокси изолирован для запросов Gemini

- Цель: обойти региональное ограничение Gemini, не направляя Telegram-трафик и остальные соединения бота через прокси.
- Причина прежней ошибки: Google Gen AI SDK наследовал системную переменную прокси со схемой `socks4://`, которую транспорт `httpx` не поддерживает.
- Что изменено: добавлена отдельная настройка `GEMINI_PROXY_URL`; системные proxy-переменные для Gemini теперь игнорируются; разрешены `http://`, `https://` и `socks5://`; для SOCKS5 добавлена зависимость `httpx[socks]`; некорректная схема или адрес дают понятную конфигурационную ошибку.
- Затронутые файлы: `src/ai_engine/llm_client.py`, `requirements.txt`, `docs/WORK_LOG.md`.
- Проверка: Python-синтаксис проверен локально; реальный запрос через пользовательский прокси ещё не выполнен.
- Статус: требует проверки.
- Следующий шаг: установить зависимости, указать `GEMINI_PROXY_URL` в локальном `.env` и повторить `python tests/run_gemini_evaluation.py`.

### 2026-07-13 — HTTP-прокси не прошёл TLS-рукопожатие

- Цель: выполнить первый из 15 синтетических запросов Gemini через локальный прокси.
- Фактический результат: схема `http://127.0.0.1:10808` была принята клиентом, но запрос остановился на TLS-рукопожатии внутри `httpcore._sync.http_proxy` и был прерван пользователем через `Ctrl+C`.
- Диагноз: порт доступен, но, вероятнее всего, не является HTTP CONNECT-прокси; требуется проверить его как SOCKS5-порт и отдельно подтвердить выходной IP.
- Статус: требует проверки.
- Следующий шаг: проверить порт командой `curl` через SOCKS5, затем установить `GEMINI_PROXY_URL=socks5://127.0.0.1:10808` и повторить оценку.

### 2026-07-13 — подготовлен тестовый перенос бота на Render Frankfurt

- Цель: выполнять запросы Gemini с серверного IP разрешённого региона и отказаться от нестабильного локального прокси.
- Что изменено: `bot.py` получил два режима — локальный polling и серверный Telegram webhook; добавлены health endpoint, секрет webhook и конфигурация порта; создан `render.yaml` для бесплатного Web Service во Франкфурте.
- Сопутствующее исправление ядра: в пустую JSON-базу добавлен обязательный ключ `student_homework`, без которого чистый серверный запуск падал бы при назначении задания.
- Ограничение: файловая система бесплатного Render непостоянна; развёртывание разрешено только для тестов без реальных ученических данных.
- Затронутые файлы: `bot.py`, `config.py`, `render.yaml`, `src/database/json_storage.py`, `docs/CLAUDE.md`, `docs/WORK_LOG.md`.
- Проверка: синтаксис и конфигурация проверяются локально; реальное развёртывание и Telegram webhook ещё не выполнены.
- Статус: требует проверки.
- Следующий шаг: синхронизировать изменения, отправить их в GitHub и создать Render Blueprint с секретами.

### 2026-07-13 — OpenRouter недоступен с локального IP

- Цель: проверить API-агрегатор как более простой путь к Gemini без переноса бота.
- Проверка: публичный endpoint OpenRouter `/api/v1/models` вызван напрямую с `trust_env=False`, то есть без системного SOCKS4-прокси.
- Фактический результат: сервис ответил HTTP `403 Forbidden`.
- Решение: не добавлять второй LLM-провайдер до подтверждения доступности; вернуться к подготовленному развёртыванию Render Frankfurt.
- Статус: проверено, вариант отклонён для текущего локального окружения.
- Следующий шаг: отправить серверные изменения в GitHub и создать Render Blueprint.

### 2026-07-13 — исправлена версия Python для сборки Render

- Цель: устранить падение первого серверного deploy на этапе установки зависимостей.
- Фактическая причина: Render по умолчанию использовал Python 3.14.3; совместимая со старым `aiogram` версия `pydantic-core` не имела готового wheel и попыталась собираться через Rust в read-only каталоге Cargo.
- Что изменено: в корень проекта добавлен `.python-version` со значением `3.12`, совпадающим с локальной рабочей веткой Python.
- Проверка: причина подтверждена логом Render; повторная серверная сборка ещё не выполнена.
- Статус: требует проверки.
- Следующий шаг: синхронизировать и отправить `.python-version` и обновлённые документы в GitHub; проверить автоматический redeploy.

### 2026-07-13 — webhook больше не блокирует запуск Render

- Цель: устранить падение процесса после успешной сборки на Python 3.12.
- Фактическая причина: Telegram получил `Failed to resolve host`, потому что регистрация webhook выполнялась до готовности DNS нового домена Render; исключение в startup не позволяло aiohttp открыть порт.
- Что изменено: регистрация webhook перенесена в фоновую задачу с увеличивающейся задержкой между повторными попытками; временная ошибка Telegram теперь записывается в лог, но не останавливает web service.
- Затронутые файлы: `bot.py`, `docs/CLAUDE.md`, `docs/WORK_LOG.md`.
- Проверка: синтаксис проверен локально; повторный deploy и фактическая регистрация webhook ещё не выполнены.
- Статус: требует проверки.
- Следующий шаг: отправить исправление в GitHub и дождаться в Render сообщений об открытом порте и зарегистрированном webhook.

### 2026-07-13 — тестовый сервер Render введён в работу

- Цель: завершить перенос Telegram-бота в разрешённый для Gemini регион.
- Фактический результат: Web Service `https://ai-tutor-platform-ycpi.onrender.com` получил статус Live; aiohttp открыл порт 10000; health-check вернул HTTP 200; после DNS-задержки Telegram webhook зарегистрирован.
- Проверка: успешный статус и регистрация webhook подтверждены логами Render и пользователем.
- Ограничение: это тестовая среда с непостоянной JSON-базой; реальные данные и фотографии учеников не подключаются.
- Результат: инфраструктурный переезд завершён; проверка качества Gemini ещё не выполнена.
- Статус: выполнено.
- Следующий шаг: запустить 15 синтетических проверок Gemini с серверного IP, не расширяя функциональность бота.

### 2026-07-13 — Telegram-сценарий подтверждён на Render

- Цель: проверить не только health endpoint, но и реальную доставку события от Telegram до обработчика бота.
- Сценарий: пользователь отправил `/start`; серверный бот ответил приветствием и запросом роли «Кто ты?».
- Проверка: Telegram → Render webhook → aiogram handler → ответ пользователю прошли end-to-end.
- Результат: серверный интерфейс бота работоспособен.
- Статус: выполнено.
- Следующий шаг: добавить закрытый способ запуска 15 синтетических Gemini-проверок с Render IP.

### 2026-07-13 — подготовлен закрытый запуск синтетической оценки

- Цель: выполнить текущую фазу Gemini с серверного IP без передачи реальных сообщений учеников.
- Что изменено: добавлена администраторская команда `/gemini_eval`; 15 фиксированных примеров вынесены из CLI-runner в общий модуль оценки; вызовы выполняются вне event loop; добавлены прогресс 5/15 и 10/15, итоговая сводка, защита от параллельного запуска и сетевой таймаут 30 секунд.
- Доступ и данные: команда сравнивает Telegram ID с `ADMIN_TELEGRAM_ID` и использует только встроенные синтетические примеры.
- Затронутые файлы: `src/ai_engine/evaluation.py`, `llm_client.py`, `src/telegram_bot/handlers/demo.py`, `bot.py`, `tests/run_gemini_evaluation.py`, проектные документы.
- Проверка: Python-синтаксис прошёл локальную проверку; полный unit-прогон в рабочем снимке недоступен без зависимостей проекта; реальный серверный запуск команды ещё не выполнен.
- Статус: требует проверки.
- Следующий шаг: развернуть изменение и отправить `/gemini_eval` с администраторского аккаунта.

### 2026-07-13 — выполнена первая серверная оценка Gemini

- Цель: проверить качество Gemini на 15 фиксированных синтетических решениях с Render Frankfurt.
- Фактический результат: обработаны 15 из 15 примеров, ошибок API нет; 14 ожидаемых статусов совпали, точность по набору составила 93,3%.
- Расхождение: правильный пример `binary_correct` (`10110₂ = 16 + 4 + 2 = 22₁₀`) модель пометила `has_error` с уверенностью 1.00.
- Вывод: транспорт и серверная интеграция Gemini работоспособны; обнаружено ложное срабатывание модели, поэтому Фаза 1 пока не закрыта.
- Статус: требует разбора качества.
- Следующий шаг: получить полный `feedback` случая `binary_correct` из логов Render и внести минимальное проверяемое исправление без ослабления эталона.

### 2026-07-13 — разобрано ложное срабатывание `binary_correct`

- Данные диагностики: Gemini сообщил о якобы неверном определении степеней двойки и присвоил `calculation_error`, хотя `10110₂ = 16 + 4 + 2 = 22₁₀` математически верно.
- Причина: ошибка рассуждения модели; сокращённая запись без нулевых слагаемых была ошибочно воспринята как неверное разложение.
- Что изменено: системный промпт теперь требует независимо перепроверять вычисления перед `has_error` и явно разрешает пропуск нулевых слагаемых в позиционных системах счисления.
- Контроль: эталон и все 15 тестовых примеров оставлены без изменений, чтобы не скрывать ошибку подстройкой набора.
- Статус: требует повторной серверной проверки.
- Следующий шаг: развернуть новый промпт и снова выполнить `/gemini_eval`; сравнить полный результат с первым прогоном 14/15.

### 2026-07-13 — Фаза 1 завершена повторным прогоном 15/15

- Цель: подтвердить исправление ложного срабатывания без изменения контрольного набора и без регрессий.
- Фактический результат: повторная команда `/gemini_eval` обработала 15 из 15 синтетических примеров; все ожидаемые статусы совпали, ошибок API и расхождений нет.
- Сравнение: первый прогон — 14/15, второй после общего исправления промпта — 15/15.
- Проверка: Telegram → Render → Gemini → структурированный результат → итоговое сообщение бота прошли end-to-end.
- Ограничение: результат подтверждает только синтетический текстовый набор; реальные ответы и фотографии учеников по-прежнему запрещено отправлять во внешний API.
- Статус: Фаза 1 выполнена.
- Следующий шаг: до начала Фазы 2 согласовать допустимые данные, тестовый набор изображений и критерий готовности; не подключать реальные фотографии автоматически.

### 2026-07-13 — подготовлен первый синтетический vision-тест

- Цель: начать Фазу 2 без передачи фотографий или данных реальных учеников.
- Тестовые данные: создано искусственное изображение тетрадного листа с правильным решением цикла Python; персональные данные, имя, дата и школа отсутствуют.
- Что изменено: `LLMClient` получил защищённый метод проверки изображения через Gemini; добавлены парсинг результата, один фиксированный image-case и администраторская команда `/gemini_image_eval`.
- Граница данных: внешний вызов требует `synthetic_test=True`; обычный пользовательский путь фотографии не принимает и не вызывает Gemini.
- Технический формат: JPEG передаётся inline через `types.Part.from_bytes`; допустимые MIME-типы ограничены форматами, поддерживаемыми Gemini.
- Проверка: изображение визуально проверено, Python-синтаксис изменённых модулей прошёл локальную проверку; реальный vision-запрос на Render ещё не выполнен.
- Статус: прототип, требует серверной проверки.
- Следующий шаг: синхронизировать изменения, развернуть их на Render и выполнить `/gemini_image_eval` с администраторского аккаунта.

### 2026-07-13 — исправлен формат мультимодального запроса Gemini

- Фактический результат первого прогона: Render отправил запрос в Gemini, но Interactions API вернул HTTP 400 `Provide a 'role' field (for Turn[]), or a 'type' field (for Step[])`.
- Диагноз: JPEG был найден и запрос дошёл до API; ошибка возникла при разборе структуры мультимодального `input`, до проверки изображения моделью.
- Что изменено: только image-метод переведён на `client.models.generate_content` с `types.Part.from_bytes` и `response_json_schema`, как предусмотрено Google Gen AI SDK.
- Защита от регрессии: проверенный текстовый Interactions-контур не изменён.
- Проверка: Python-синтаксис исправления проверяется локально; повторный vision-запрос на Render ещё не выполнен.
- Статус: требует повторной серверной проверки.
- Следующий шаг: развернуть исправление и снова выполнить `/gemini_image_eval`.

### 2026-07-13 — первый vision-сценарий прошёл end-to-end

- Цель: подтвердить реальную проверку синтетического рукописного решения через Gemini на Render.
- Фактический результат: `/gemini_image_eval` завершился без API-ошибок; пример `loop_handwritten_correct` получил ожидаемый статус `correct` с уверенностью 1.00.
- Качество ответа: Gemini правильно объяснил семантику `range(1, 11)` и предложил уместный следующий вопрос про вывод чётных чисел.
- Проверенный путь: встроенный JPEG → Render → Gemini `generateContent` → JSON Schema → парсер → Telegram-сообщение администратору.
- Граница данных: использовалось только искусственное изображение без персональных данных; пользовательский приём реальных фото не подключён.
- Статус: первый vision-прототип проверен; Фаза 2 продолжается.
- Следующий шаг: создать небольшой фиксированный набор синтетических изображений со статусами `correct`, `has_error` и `unclear`, затем считать качество так же, как в текстовой Фазе 1.

### 2026-07-13 — подготовлен vision-набор из 5 изображений

- Основание: базовый `loop_handwritten_correct` повторно получил `correct` с уверенностью 1.00 и корректной обратной связью.
- Набор: правильный цикл, цикл с ошибкой правой границы, правильный перевод двоичного числа, ошибочный перевод и намеренно нечитаемое решение.
- Эталоны: два `correct`, два `has_error`, один `unclear`; изображения и ожидаемые статусы зафиксированы до запуска.
- Что изменено: `/gemini_image_eval` последовательно проверяет все 5 fixture, считает совпадения и API-ошибки, логирует подробные результаты и отправляет итоговую сводку.
- Граница данных: все изображения искусственные и не содержат имён, дат, школы или иных персональных данных; пользовательские фото не подключены.
- Проверка: изображения просмотрены, файлы существуют, Python-синтаксис модулей проверяется локально; серверный прогон набора ещё не выполнен.
- Статус: требует проверки.
- Следующий шаг: развернуть изменения и выполнить `/gemini_image_eval`; для каждого расхождения получить `feedback`, `hint` и `error_type` из логов Render.

### 2026-07-13 — выполнен первый vision-benchmark 4/5

- Фактический результат: Gemini обработал 5 из 5 синтетических JPEG без API-ошибок; 4 ожидаемых статуса совпали, точность набора составила 80%.
- Расхождение: `unreadable_handwritten_solution` ожидал `unclear`, но получил `correct` с уверенностью 0.90.
- Предварительная интерпретация: модель могла либо распознать часть сильно размытого кода, либо восстановить предполагаемое решение по известному условию; итоговая причина требует полного `feedback` из лога.
- Решение: не менять эталон, изображение или промпт до разбора объяснения модели.
- Статус: требует диагностики качества.
- Следующий шаг: извлечь из Render полную строку `Gemini synthetic image evaluation result` для `unreadable_handwritten_solution` и проверить поля `feedback`, `hint`, `error_type`.

### 2026-07-13 — vision-проверка разделена на транскрипцию и оценку

- Диагноз подтверждён логом: для нечитаемого fixture Gemini описал «считывание числа» и «умножение», то есть восстановил предполагаемое решение по условию вместо честного отказа.
- Риск: единый мультимодальный запрос позволяет модели использовать условие задачи для заполнения неразличимых фрагментов и затем уверенно проверить собственную догадку.
- Что изменено: первый вызов теперь видит только изображение и возвращает буквальную транскрипцию, `legibility` и уверенность; условие задачи ему не передаётся.
- Защитное правило: `partial` и `unreadable` локально превращаются в `unclear`, а текстовая проверка для них вообще не вызывается; только `readable` идёт во второй, уже проверенный текстовый контур.
- Наблюдаемость: результат benchmark теперь сохраняет `image_legibility` и `image_transcription` в логах.
- Проверка: добавлен unit-сценарий, подтверждающий, что нечитаемое изображение не передаётся в проверку решения; серверный прогон ещё не выполнен.
- Статус: требует повторной проверки 5 изображений.
- Следующий шаг: развернуть изменение, выполнить `/gemini_image_eval` и проверить как итоговый статус, так и транскрипцию каждого расхождения.

### 2026-07-13 — защитный vision-benchmark завершён 5/5

- Цель: подтвердить двухэтапную схему и устранить уверенную проверку додуманного текста.
- Фактический результат: 5 из 5 ожидаемых статусов совпали, ошибок API и расхождений нет.
- Критический контроль: намеренно размытый fixture теперь получил ожидаемый `unclear`; его содержимое не было передано во второй этап проверки решения.
- Регрессии: четыре читаемых изображения сохранили ожидаемые статусы `correct` и `has_error` после добавления отдельной транскрипции.
- Результат: синтетический vision-контур «изображение → транскрипция/читаемость → текстовая проверка → Telegram» проверен end-to-end.
- Ограничение: пользовательская загрузка фотографий ещё не подключена; реальные данные учеников не обрабатываются.
- Статус: синтетический benchmark выполнен, Фаза 2 продолжается.
- Следующий шаг: согласовать политику данных и минимальный администраторский канал загрузки только явно синтетических тестовых изображений.

### 2026-07-13 — подготовлена администраторская загрузка синтетического фото

- Решение по данным: разрешён только администраторский тестовый канал и только для изображения, явно помеченного командой как синтетическое; реальные фото учеников не разрешены.
- Интерфейс: фото отправляется с подписью `/check_synthetic_photo текст задания`.
- Обработка: Telegram-файл скачивается в `BytesIO`, проверяется ограничение 5 Мбайт и запускается двухэтапная цепочка «транскрипция без условия → текстовая проверка».
- Хранение: код не создаёт файл на диске, не пишет изображение в `uploads`, JSON или базу; байты существуют только в памяти фоновой задачи.
- Доступ: обработчик повторно проверяет `ADMIN_TELEGRAM_ID`; обычное фото без специальной подписи не вызывает Gemini через этот канал.
- Наблюдаемость: администратору возвращаются `legibility`, буквальная транскрипция и педагогический результат без логирования байтов или Telegram file ID.
- Проверка: синтаксис и unit-тесты парсинга подписи выполняются локально; реальная загрузка через Telegram → Render ещё не проверена.
- Статус: требует серверного end-to-end прогона.
- Следующий шаг: развернуть изменение и отправить искусственное фото с подписью-командой; подтвердить отсутствие новых файлов и правильный результат.

### 2026-07-13 — администраторская загрузка фото прошла end-to-end

- Вход: администратор отправил синтетическое фото ошибочного перевода `10110₂ = 16 + 8 + 2 = 26₁₀` с текстом задания в подписи-команде.
- Транскрипция: Gemini вернул `readable` и сохранил существенные числа и операции, включая ошибочные `+ 8` и `26`.
- Проверка: второй этап вернул `has_error`, указал ошибку в вычислении степеней двойки и дал наводящий вопрос без готового полного решения.
- Технический путь: Telegram photo → `BytesIO` → транскрипция без условия → текстовая проверка → ответ администратору прошёл без API-ошибок.
- Хранение: код канала не создаёт локальный файл и не записывает изображение или результат в JSON/`learning_dna`.
- Результат: техническое ядро Фазы 2 подтверждено в синтетической границе.
- Ограничение: реальные фото учеников, пользовательский rollout и долговременное хранение не разрешены.
- Статус: выполнено.
- Следующий шаг: до Фазы 3 отдельно определить, какие результаты допустимо сохранять, на каком основании и с каким сроком хранения.

### 2026-07-13 — согласована политика хранения v1

- Текущее решение: исходное фото не сохраняется; транскрипция не записывается; разрешено сохранять только тему, статус, уверенность и тип ошибки синтетического администраторского теста.
- Реальные данные: результаты учеников не сохраняются и не передаются в новый контур без отдельного явного решения.
- Изменяемость: политика может быть пересмотрена по ходу проекта, но новая версия должна быть зафиксирована до изменения кода и начала обработки расширенного состава данных.
- Старые записи: при смене политики отдельно определяется, нужно ли их сохранить, мигрировать или удалить; новая политика не применяется к ним молча.
- Статус: решение принято, реализация сохранения ещё не выполнена.
- Следующий шаг: проверить существующий контракт `learning_dna` и добавить минимальную запись только разрешённых структурированных полей для синтетического администраторского сценария.

### 2026-07-13 — подготовлено минимальное сохранение в `learning_dna`

- Проверка существующего кода: общий `update_learning_dna_after_check` сохраняет сигналы, рекомендации, XP и другие данные, поэтому он не соответствует узкой политике v1 и не используется в синтетическом сценарии.
- Реализация: в существующем разделе `learning_dna` добавлен зарезервированный журнал `_synthetic_admin_checks`; отдельная запись содержит только `topic`, `status`, `confidence`, `error_type`.
- Защита: запись собирается по whitelist, а не копированием результата; фото, байты, транскрипция, условие и Telegram ID технически не проходят в JSON.
- Подключение: сохранение вызывается только после администраторской команды `/check_synthetic_photo`; встроенный benchmark и пользовательские пути не записываются.
- Проверка: 3 теста хранилища и 4 теста подписи прошли; отдельный тест передал лишние секретные поля и подтвердил их отсутствие в JSON.
- Наблюдаемость: после записи бот читает синтетический журнал обратно и сообщает точные сохранённые поля и количество записей.
- Ограничение: JSON-файл бесплатного Render непостоянен и может исчезнуть после redeploy; текущая цель — проверить контракт, а не долговременное хранение.
- Статус: локально проверено, требует серверного end-to-end прогона.
- Следующий шаг: развернуть изменение и повторить загрузку синтетического фото; проверить сообщение `Сохранено по политике v1` и отсутствие лишних полей.

### 2026-07-13 — первая синтетическая запись сохранена в `learning_dna`

- Вход: администраторский синтетический пример с ошибочным переводом двоичного числа.
- AI-результат: `topic=Системы счисления`, `status=has_error`, `confidence=1.00`, `error_type=calculation_error`.
- Хранение: репозиторий подтвердил запись по политике v1 и чтение журнала обратно; количество записей — 1.
- Минимизация: Telegram-ответ показывает только четыре разрешённых поля; фото, транскрипция и условие использовались для обработки, но не вошли в сохраняемую запись.
- Ограничение: проверено временное JSON-хранилище текущего экземпляра Render, а не долговременная база.
- Статус: одиночная запись выполнена end-to-end; накопление требует второго прогона без redeploy.
- Следующий шаг: загрузить второй синтетический пример и подтвердить счётчик 2, затем зафиксировать завершение синтетического контракта Фазы 3.

### 2026-07-13 — накопление синтетических результатов подтверждено

- Условие проверки: второй пример отправлен без commit, redeploy или перезапуска, поэтому использовался тот же временный `database.json` Render.
- Вторая запись: `topic=Системы счисления`, `status=correct`, `confidence=1.00`, `error_type=None`.
- Чтение после записи: бот сообщил 2 записи, подтвердив сохранение первой и добавление второй, а не перезапись журнала.
- Контракт данных: обе записи содержат только четыре поля политики v1; фото, условие и транскрипция не сохранялись.
- Наблюдение OCR: нижние индексы второй фотографии были потеряны в текстовом представлении (`101102`, `2210`), но модель корректно оценила решение по контексту задачи.
- Статус: синтетическая часть Фазы 3 выполнена end-to-end.
- Ограничение: реальное ученическое хранение не разрешено; JSON Render остаётся временным.
- Следующий шаг: зафиксировать изменения в Git, затем выбрать долговременное тестовое хранилище либо отдельно спроектировать политику v2 до работы с реальными данными.

### 2026-07-13 — подготовлен PostgreSQL-backend синтетического журнала

- Граница миграции: переносится только `_synthetic_admin_checks`; пользователи, задания и другие разделы JSON пока не меняются.
- Переключение: при наличии `DATABASE_URL` чтение и запись синтетического журнала идут в PostgreSQL, без переменной сохраняется локальный JSON-режим.
- Минимизация: PostgreSQL получает тот же объект политики v1 из четырёх полей; исходное фото, транскрипция, условие и Telegram ID в backend не передаются.
- Надёжность: добавление выполнено одной атомарной PostgreSQL-командой, поэтому параллельные проверки не должны перезаписывать журнал.
- Схема: служебная таблица `app_state` создаётся автоматически, журнал хранится как JSONB-массив под отдельным ключом.
- Зависимость: добавлен `psycopg[binary]`; в Render объявлена секретная переменная `DATABASE_URL`.
- Проверка: 5 изолированных тестов хранения проходят, включая JSON fallback, whitelist, append и маршрутизацию чтения/записи в PostgreSQL.
- Решение по старым данным: две временные синтетические записи не мигрируются автоматически; после подключения базы выполняется новая проверка накопления.
- Статус: код готов, серверное подключение PostgreSQL и проверка сохранности после redeploy ещё не выполнены.
- Следующий шаг: создать тестовую PostgreSQL-базу, установить её внутренний URL в `DATABASE_URL`, развернуть код и выполнить две синтетические проверки с redeploy между ними.

### 2026-07-13 — установлен дедлайн бесплатного PostgreSQL-теста

- Решение: использовать Free PostgreSQL для синтетического прогона в пределах 30-дневного срока.
- Контрольная дата: 2026-08-04, 23-й день теста при создании базы 2026-07-13.
- На контрольной дате: проверить результаты, ошибки и сохранность после redeploy; если контракт подтверждён, обновить существующую базу до платного тарифа.
- Ограничение остаётся: реальные ученические данные до политики v2 не сохраняются.
- Напоминание создано.

### 2026-07-13 — долговременное PostgreSQL-хранение подтверждено

- До контрольного redeploy бот прочитал и дополнил PostgreSQL-журнал до 2 записей.
- Web Service был повторно развернут командой `Deploy latest commit`; PostgreSQL как отдельный ресурс не пересоздавался.
- После redeploy следующая синтетическая проверка вернула размер журнала 3.
- Вывод: записи переживают замену контейнера приложения, чтение и атомарное добавление через `DATABASE_URL` работают end-to-end.
- Политика v1 сохранена: в журнал передаются только `topic`, `status`, `confidence`, `error_type`; реальные данные учеников не разрешены.
- Статус: Фаза 3a.1 выполнена.
- Следующий шаг: продолжать синтетический 23-дневный прогон, собирать ошибки и не расширять ядро до определения следующего обязательного шага пользовательского цикла.

### 2026-07-14 — закрытый student-first текстовый цикл подтверждён

- Первый запуск `/student_cycle_demo` выявил интеграционный дефект: генератор выдавал пять описаний-заглушек без условий, поэтому объективная AI-проверка была невозможна.
- Исправление: в существующем `homework_generator.py` добавлен конкретный проверяемый шаблон для темы «Условные операторы»; остальные темы не расширялись.
- Контроль доступа: реальный Gemini-вызов в ученическом обработчике разрешён только `ADMIN_TELEGRAM_ID`; обычному пользователю возвращается блокирующее сообщение без сохранения результата.
- Правильный прогон: программа с `if / elif / else` получила `status=correct`, `confidence=1.00`; PostgreSQL вырос с 3 до 4 записей.
- Проверка статуса: назначение прошло `new → submitted → checked`, что подтверждено повторным открытием `📚 Моё ДЗ`.
- Ошибочный прогон: решение без отдельной ветки `x == 0` получило `status=has_error`, `error_type=logic_condition_error`, точную подсказку и увеличило журнал до 5 записей.
- Минимизация: сохраняются только `topic`, `status`, `confidence`, `error_type`; Learning DNA и педагогическое решение в этом закрытом сценарии намеренно не записываются до политики v2.
- Автотесты: новый тест проверяет, что задание содержит конкретный вход и ожидаемые результаты, а не текст-заглушку; вместе с тестами хранения прошло 6 тестов.
- Статус: Фаза 3a.2 выполнена end-to-end.
- Следующий шаг: определить минимальную политику v2 и правовое/продуктовое основание до открытия текстовой проверки реальному ученику; фото учеников остаются вне следующего шага.

### 2026-07-14 — подготовлено сравнение трёх текстовых AI-провайдеров

- Проверены актуальные условия Gemini API: приложение, ориентированное на пользователей младше 18 лет или вероятно доступное им, нельзя строить на Gemini API. Поэтому Gemini остаётся только в синтетическом администраторском контуре.
- Выбран следующий безопасный шаг: сравнить Gemini, Mistral и YandexGPT на существующих 15 синтетических текстовых решениях до открытия реальных данных.
- Добавлен общий контракт текстового провайдера и отдельные адаптеры Gemini, Mistral и YandexGPT; Mistral и YandexGPT используют OpenAI-совместимый Chat Completions API через уже установленный `httpx`.
- Защита продублирована на уровне каждого нового адаптера: без `synthetic_test=True` HTTP-запрос не создаётся.
- Добавлены переменные окружения Mistral/Yandex в `render.yaml`, консольный runner `tests/run_provider_comparison.py` и администраторская команда `/provider_eval`.
- Существующий `/gemini_eval` и мультимодальный Gemini-контур не заменены и не расширены.
- Проверка в рабочем окружении ассистента: изменённые Python-файлы проходят `py_compile`; полный unittest-прогон здесь невозможен из-за отсутствия проектных зависимостей `google-genai` и `httpx` в системном Python.
- Статус: код подготовлен, но результат benchmark ещё не подтверждён end-to-end. Требуются синхронизация, тесты в `.venv`, API-ключи Mistral/Yandex и запуск `/provider_eval` на Render.

### 2026-07-15 — подготовлен обратимый пилот реальных фото policy v2

- Граница пилота: только Qwen Vision и только при `QWEN_PILOT_V2_ENABLED=true`; значение по умолчанию — `false`.
- Фото загружается в память процесса без записи в `uploads/` или другую локальную папку.
- В AI-запрос программно не добавляются имя и Telegram ID; передаётся само фото, поэтому ученик не должен размещать на листе ФИО и другие персональные данные.
- AI-результат считается черновиком: ученику автоматический вердикт не отправляется, фото и черновик получает администратор-преподаватель в Telegram.
- Назначение остаётся в статусе `submitted`; Learning DNA, итоговая оценка и policy v1 журнал не обновляются до подтверждения преподавателем.
- Ограничение: копия фото остаётся в инфраструктуре Telegram, а изображение обрабатывает внешний Qwen; это не self-hosted и не режим полной приватности.
- Защита: лимит фото 5 Мбайт; отдельный `pilot_v2` не открывает реальные данные другим провайдерам.
- Проверка в среде ассистента: изменённые Python-файлы прошли `py_compile`; полный unittest требует проектные зависимости `google-genai` и `aiogram` и должен быть запущен в `.venv` пользователя.
- Следующий шаг: прогнать один контролируемый пример, оценить удобство преподавателя и только затем решать, нужны ли кнопки подтверждения и отдельный журнал policy v2.

### 2026-07-16 — подготовлен синтетический benchmark Kimi K2.6

- Kimi добавлен отдельным провайдером Moonshot API и не заменяет Qwen в рабочем или пилотном контуре.
- Команда `/kimi_eval` прогоняет неизменённые 15 текстовых и 5 визуальных синтетических примеров.
- Используется `kimi-k2.6`: универсальная мультимодальная модель; K2.7 Code пока не подключается, чтобы сначала получить сопоставимый базовый результат.
- Ответ ограничен существующей JSON Schema; thinking отключён, а несовместимый с K2.6 параметр `temperature=0.1` не отправляется.
- Защита данных сохранена: без `synthetic_test=True` Kimi блокирует текст и изображения до HTTP-запроса.
- Конфигурация Render: секрет `MOONSHOT_API_KEY`, модель `KIMI_MODEL=kimi-k2.6`.
- Статус: код подготовлен; итоговые показатели фиксируются только после полного запуска на Render.

### 2026-07-16 — в тот же benchmark добавлен MiniMax M3

- Официальный model ID: `MiniMax-M3`; endpoint: OpenAI-совместимый `https://api.minimax.io/v1/chat/completions`.
- Команда `/minimax_eval` использует те же 15 текстов и 5 изображений, что Kimi и Qwen.
- Для стабильного ответа thinking отключён; используется JSON Object Mode, после которого действует существующая строгая локальная валидация полей и семантики.
- Реальные данные блокируются до HTTP-запроса; MiniMax не подключён к ученическому или policy v2 контуру.
- Конфигурация Render: секрет `MINIMAX_API_KEY`, модель `MINIMAX_MODEL=MiniMax-M3`.

### 2026-07-16 — Kimi benchmark переключён на бесплатный OpenRouter

- Причина: прямой Moonshot API подтвердил исправность ключа, но вернул `429 exceeded_current_quota_error` из-за нулевого баланса.
- Новый маршрут: OpenRouter `moonshotai/kimi-k2.6:free`; используется только для синтетических тестов.
- Конфигурация: `OPENROUTER_API_KEY` и `KIMI_MODEL=moonshotai/kimi-k2.6:free`.
- Защита лимита: benchmark прекращается после первой ошибки HTTP 429 вместо продолжения остальных заведомо неуспешных запросов.
- Ограничение: бесплатный endpoint может иметь очередь и суточные лимиты; его результат оценивает качество модели, но не производственную доступность.

### 2026-07-17 — преподавательский черновик policy v2 стал диагностическим

- Практический пилот безопасно отправил сомнительный результат преподавателю, но прежнее сообщение `Не удалось уверенно проверить решение` не объясняло причину.
- В карточку только для преподавателя добавлены читаемость изображения, уверенность, AI-статус, тип ошибки и распознанный текст.
- Ученик по-прежнему получает только подтверждение передачи; автоматический вердикт, транскрипция и технические поля ему не отправляются.
- Транскрипция ограничена по длине, чтобы диагностическая карточка помещалась в лимит Telegram.
- Следующий критерий: повторить то же фото и определить, возник `unclear` из-за OCR, ответа модели или строгой семантической валидации.

### 2026-07-18 — начат parent-first канал фотографирования работы

- Новая продуктовая граница: фото отправляет совершеннолетний родитель, ребёнок не имеет аккаунта и не получает AI-контент; готовый анализ предназначен учителю.
- В меню родителя добавлена кнопка `📷 Сдать работу ребёнка` и отдельное FSM-состояние ожидания фотографии.
- Перед приёмом фото локально, без внешнего AI, проверяются минимальное разрешение, яркость, контраст и резкость.
- Некачественный снимок не принимается и сопровождается конкретными причинами пересъёмки; качественный получает случайный `submission_id`.
- На первом срезе принятое фото пересылается администратору-преподавателю через Telegram, родитель видит только `Работа принята`; AI-анализ, БД очереди и Learning DNA ещё не запускаются.
- Доступ к сценарию проверяется по зарегистрированной роли `parent`; текст фиксирует подтверждение совершеннолетия и статуса родителя/представителя.
- Добавлена зависимость Pillow и четыре теста качества изображения; тесты прошли локально. Python-файлы прошли `py_compile`.
- Ограничение: это прототип маршрута, Telegram остаётся временным транспортом, очередь ещё не переживает redeploy и ручной end-to-end прогон не выполнен.
- Следующий шаг после ручного прогона: сохранить метаданные submission в PostgreSQL и только затем подключать автоматический AI-worker.
